import HeroSection from "@/components/sections/heroSection";

export default function Home() {
  return (
    <main className="min-h-screen bg-brand-light">
      {/* We will add Navbar here next */}
      <HeroSection />
      {/* We will add ProductGrid here next */}
    </main>
  );
}